# Copiar estas variables a .env.local y reiniciar el servidor (npm run dev).
#
# KATIA_USE_DEMO_DB=1  →  Ignora Supabase aunque tengas URL/keys en .env;
#                         la app usa solo datos de prueba (lib/demo-store).
#                         Login local: test@test.com / test1234
#
# No usar en producción.

KATIA_USE_DEMO_DB=1
